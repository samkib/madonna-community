# TODO

- [ ] Update `src/components/Sidebar.jsx`:
  - [ ] Replace `NAV_ITEMS` with the provided array (including route roles).
  - [ ] Add/adjust `lucide-react` imports to include only: `LayoutDashboard, Megaphone, Wallet, Pin, Wrench, MessageSquareWarning, Lightbulb, Building2, Settings, X`.
  - [ ] Ensure `Sidebar.jsx` compiles (no unused/missing imports, icons used match NAV_ITEMS).
- [ ] Run a quick build/lint (if available) to verify changes.
